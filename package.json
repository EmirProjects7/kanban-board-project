{
  "name": "kanban-board-project",
  "private": true,
  "scripts": {
    "db": "docker compose -f backend/docker-compose.yml up -d --wait",
    "dev": "npm run db && concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\""  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}